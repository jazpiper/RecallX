# Changelog

## 1.0.0

- shipped the first public Memforge desktop release for macOS and Linux
- added packaged desktop tray/status polish with direct Settings, server status, workspace status, and MCP launcher access
- published the npm CLI/MCP distribution path with `memforge`, `pnw`, and `memforge-mcp`
- finalized the current renderer/API surface around Guide, Recent, Graph, Project map, Governance, and Settings
- shipped the local semantic sidecar with `local-ngram` / `chargram-v1` embedding version `2`
